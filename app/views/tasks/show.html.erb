<h2><%=@task.title %></h2>
<p><%=@task.details %></p>

<p>
<% if @task.completed %>
 <p><b>complete<b></p>
 <% else %>
 <p><b>not complited<b></p>
<% end %> </p>

<%= link_to "Delete", task_path(@task), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%>
<%=link_to "back to menue", tasks_path %>
